# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
vite.config.ts.timestamp-*.mjs

# Netlify (not needed — on GitHub Pages now)
.netlify/
nETLify/
nEtlify.toml
netlify.toml

# ── INTERNAL DOCS — NEVER COMMIT TO PUBLIC REPO ──────────────
# Strategy, pitch, and investor materials
*.xlsx
*.pptx
*.docx
*.key
archive/
dist/

# Pitch decks — internal versions only
ikwe_pitch_deck_v*.html
ikwe_investor_pitch_v*.html
ikwe_investor_deck*.html
ikwe_pitch_A*.html
ikwe_pitch_mentor*.html
ikwe_pitch_investor*.html
ikwe_deck_*.html
CANONICAL_*.html
CANONICAL_*.pptx
CANONICAL_*.pdf
CANONICAL_*.docx

# Strategy and internal docs
Ikwe_Strategic_Integration_Brief*.docx
Ikwe_Strategy_Map*.html
Ikwe_GTM_Brief*.docx
Ikwe_IP_Brief*.docx
Ikwe_Pitch_Critique*.docx

# Deploy artifacts
deploy-*.zip
ikwedecks.zip
v3pitcj.zip
7.zip
_local-archive/

# Orphaned build-copy folder removed from the deploy (Jul 15, 2026) — publish dir is repo root
public/
_to_delete/

# Netlify leftovers: ikwe.ai is on GitHub Pages, which never reads these.
# Kept locally for reference, not deployed.
_redirects
_headers

# Secrets — never commit (2026-07-27 security pass)
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
*.pem
*.key
id_rsa*
.netrc
.npmrc

# v2 preview folder — never deploy a draft site from the repo root
v2/
