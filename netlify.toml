# Netlify configuration for ikwe.ai (React + Vite)
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command   = "npm run build"
  publish   = "dist"

[build.processing]
  skip_processing = false

# Prerendering: serves crawlers fully-rendered HTML so every page
# gets its own unique <title>, <meta description>, and canonical URL.
# Required for SPA SEO — Netlify Pro plan, no extra cost.
[prerendering]
  enabled = true

[functions]
  directory    = "netlify/functions"
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options             = "DENY"
    X-Content-Type-Options      = "nosniff"
    X-XSS-Protection            = "1; mode=block"
    Referrer-Policy             = "strict-origin-when-cross-origin"
    Permissions-Policy          = "camera=(), microphone=(), geolocation=()"

# Allow reports and forms to be embedded as iframes (for sample previews on /audit)
[[headers]]
  for = "/reports/*"
  [headers.values]
    X-Frame-Options             = "SAMEORIGIN"
    X-Content-Type-Options      = "nosniff"

[[headers]]
  for = "/forms/*"
  [headers.values]
    X-Frame-Options             = "SAMEORIGIN"
    X-Content-Type-Options      = "nosniff"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
